DATABASE_URL="postgresql://user:password@localhost:5432/vitalis?schema=public"
JWT_ACCESS_SECRET="change-me"
JWT_REFRESH_SECRET="change-me-too"
PORT=3000
# Opcional: restringe CORS a este origen exacto en producción (si no se define, acepta cualquiera)
# FRONTEND_URL="https://tu-frontend.up.railway.app"

REDIS_URL="redis://localhost:6379"

# Stripe test mode — dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY="sk_test_..."
# Se obtiene con `stripe listen --forward-to localhost:3000/webhooks/stripe`
STRIPE_WEBHOOK_SECRET="whsec_..."

# Resend — resend.com/api-keys
RESEND_API_KEY="re_..."
EMAIL_FROM="citas@vitalis-clinic.test"

# Opcional: si no se define, Sentry no se inicializa (útil en dev/test)
# SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"

HOLD_TTL_MINUTES=15
REMINDER_HOURS_BEFORE=24
